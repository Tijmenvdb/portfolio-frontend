import { CommentSectionOrderPipe } from './comment-section-order.pipe';

describe('CommentSectionOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentSectionOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
